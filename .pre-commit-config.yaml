# Secret scanning — Capa 1 (local, antes de cada commit)
# Instalar: pip install pre-commit && pre-commit install
# Escaneo manual: pre-commit run --all-files
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
