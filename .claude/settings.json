{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "printf '# Repository governance — CLAUDE.md (auto-loaded at session start)\\n\\n'; cat \"${CLAUDE_PROJECT_DIR:-.}/CLAUDE.md\"",
            "statusMessage": "Loading repository governance (CLAUDE.md)"
          }
        ]
      }
    ]
  }
}
